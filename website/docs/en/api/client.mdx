# Client API

Client API is exposed in `@paretojs/core/client`.

##  PageContext

PageContext is a provider that must wrap around `client-entry.tsx`,
providing support for `react-async-helmet` and `critical-css`.